# BGMファイルの置き場所
# ファイル名と対応ページ:
#   bgm_home.mp3        → / (入口)
#   bgm_observation.mp3 → /observation (観測室)
#   bgm_archive.mp3     → /archive (記録室)
#   bgm_exit.mp3        → /exit (境界域)
#   bgm_null.mp3        → /null (崩落域)
# フォーマット: mp3 または ogg 推奨
